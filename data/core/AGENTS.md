# AGENTS.md

这是一个本地 LLM Wiki。先读 `wiki/index.md`，再沿 `[[wikilinks]]` 导航。

## 边界

- `raw/logseq-import/` 与 `wiki/insights/` 只读。
- 新增页面时更新 `wiki/index.md`，任何 Wiki 变更追加 `wiki/log.md`。
- 页面使用中文、英文 kebab-case 文件名和完整 YAML frontmatter。
- 一个概念一个页面，超过 500 行时拆分。
