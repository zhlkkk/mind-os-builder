# LLM Wiki Schema

目录包括 `raw/`、`wiki/`、`journals/` 与 `templates/`。概念页必须包含 `domain`、`sources`、`created`、`updated`、`tags`，内部链接使用 `[[wikilinks]]`。

`wiki/index.md` 是导航入口，`wiki/log.md` 记录变更。`wiki/insights/` 由人类独占写入，`raw/logseq-import/` 是不可修改的历史导入。
