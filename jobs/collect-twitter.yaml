version: v1
name: Twitter 候选采集
inputs:
  vault: /绝对路径/Mind-OS
command: [mindos, collect, twitter, prepare, "{vault}", --json]
effects: [workspace.read, network.read]
concurrency: single
retry: {max_attempts: 2}
schedule: hourly
